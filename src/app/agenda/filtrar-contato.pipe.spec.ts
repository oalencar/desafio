import { FiltrarContatoPipe } from './filtrar-contato.pipe';

describe('FiltrarContatoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarContatoPipe();
    expect(pipe).toBeTruthy();
  });
});
